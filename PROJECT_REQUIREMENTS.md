# 插图 + 文字故事阅读系统：需求与设计基线

> 本文档用于跨电脑、跨开发工具持续开发。任何需求、范围或核心模型的变更，都应同步更新本文档。
>
> 文档状态：需求基线 v0.2（2026-08-12）

交互原型：[素材管理、人物设定与 AI 参考选择](prototypes/asset-management.html)

## 1. 项目目标

建设一个以“插图 + 文字”为主要呈现形式的故事系统，包含管理端和使用端。未来允许加入视频内容。

- 管理端：维护完整故事设定、组织创作内容与素材，并在统一设定约束下通过 AI 辅助生成故事和图片。
- 使用端：面向普通读者，公开浏览、筛选、预览和阅读故事，并查看或下载允许公开的 CG。

仓库采用四项目结构：

- `story-admin-server`：管理端 Java 后端
- `story-admin-web`：管理端 Vue 前端
- `story-user-server`：使用端 Java 后端
- `story-user-web`：使用端 Vue 前端

## 2. 核心设计原则

### 2.1 设定具有继承层级

```text
系列全局设定
  ↓ 必须遵守
篇章局部设定
  ↓ 必须遵守
章节 / 场景设定
  ↓
AI 生成文字、插图及未来的视频
```

篇章可以补充系列设定，但不能静默违背上级设定。发生冲突时，管理端应提示管理员选择“修改全局设定”或“登记为剧情例外”。

### 2.2 创作资料与公开资料分离

详细设定、AI 提示词、剧情秘密等仅供管理端和 AI 创作使用；使用端只展示明确标记为公开的信息。人物等资料应支持公开简介、内部设定和剧透信息的区分。

### 2.3 内容采用可扩展的内容块

故事页面不固定为“一张图 + 一段文字”，而由可排序的内容块组成，首期支持标题、正文、对话、插画和分隔内容，未来可加入视频、音频、动画和人物卡片。

### 2.4 AI 生成必须可追溯

保存生成时使用的上下文、模型、参数、提示词、原始结果、人工修改及版本状态。必须能区分 AI 原稿、人工修改稿、当前采用稿和已发布稿，并支持历史比较与恢复。

## 3. 管理端需求

管理端是带有 AI 约束能力的故事创作和资产管理平台，而不是简单的文章发布后台。

### 3.1 故事系列

系列是最高层级，包含：

- 名称、简介、封面、题材、主题标签和目标读者
- 整体世界观、时代背景和世界运行规则
- 社会制度、科技、魔法或能力体系
- 整体文风、叙事视角和语言风格
- 内容限制、创作禁区和 AI 生成约束
- 故事主线、最终目标及整体时间线
- 核心人物、地点、组织、种族和物品
- 连载、完结、草稿、已发布等状态

### 3.2 世界设定库

每个系列拥有独立设定库，包括：

- 人物及人物关系
- 地点及地点关系
- 组织、阵营、种族和生物
- 关键物品
- 能力、魔法或科技体系
- 历史事件和世界时间线
- 专有名词与术语

#### 3.2.1 人物基础属性

人物是独立的业务实体，拥有系统内部唯一 ID 和面向管理者使用的人物编号。人物编号在所属故事系列内唯一，创建后默认不随名称变化，用于检索、引用和 AI 生成记录追溯。

人物基础属性至少包括：

- 人物编号、姓名和别名
- 性别
- 年龄或年龄阶段
- 种族
- 身份或职业，例如“女怪盗”
- 公开简介和内部创作说明

外貌、性格、语言习惯、动机、目标、能力、弱点、人际关系、成长轨迹和禁止行为等信息，可以继续作为人物属性或扩展设定维护。

#### 3.2.2 可扩展人物设定项

除基础属性外，每个人物可以添加任意数量的设定项，用于描述和约束具体特征。例如给“女怪盗”添加标题为“帽子”和“眼罩”的两个设定项。

每个设定项至少包括：

- 设定项 ID
- 标题，例如帽子、眼罩、发型、武器或特殊标记
- 文字说明
- 排序值
- 启用状态
- 一个或多个关联素材及其版本

设定项不应被设计成帽子、眼罩等固定数据库字段，管理员可以按不同人物和故事自由增加新标题。一个设定项允许关联多份参考素材，以表达不同视角、细节或候选方案。

在设定项中选择素材时，管理员可以从素材库选择；如果还没有合适素材，可以直接上传。上传成功后，该文件必须先创建为素材库中的正式素材，再与当前人物及设定项建立引用关系，不能成为只存在于人物页面的孤立文件。

删除人物设定项或解除素材关联时，不直接删除素材库原文件。删除素材前需要检查其他人物、设定项和生成记录是否仍在引用。

#### 3.2.3 人物 AI 生成规则

生成一张涉及人物的图片时，管理员先选择人物，再决定本次生成采用哪些人物特征和设定项：

- 基础属性中可以选择部分特征参与生成，也可以不选
- 未选择的可选特征视为“未约束”，由 AI 根据其余上下文自行决定，而不是自动继承该字段当前值
- 选择某个特征或设定项后，系统必须把它的文字说明和关联素材作为本次生成约束
- 若选中的设定项存在多份素材，应允许选择本次具体引用哪些素材；未进一步选择时默认引用该设定项的全部有效素材
- 添加并选中的设定越多，提交给 AI 的人物描述和参考素材越完整，生成结果应越具体
- UI 应清楚区分“已选择并约束”“未选择，由 AI 决定”和“没有填写”三种状态

人物首次生成用于确立基础形象，默认选择该人物全部已启用的基础属性和全部已启用设定项，管理员仍可以在提交前取消某项。首次生成采用所有设定，是为了尽可能形成完整、稳定的人物基准形象。

人物已有基准立绘后，后续生成应允许把基准立绘与本次选中的表情、服装、帽子、眼罩等设定素材组合为参考。每次生成记录必须保存人物 ID、人物编号、所选基础属性、所选设定项、实际素材版本，以及哪些特征交由 AI 自行决定。

### 3.3 篇章、章节、场景和故事页面

- 篇章隶属于系列，继承系列设定并可拥有局部人物、背景、故事线和约束。
- 篇章记录标题、简介、封面、目标、开始与结束状态、出场人物、地点和时间范围。
- 篇章下划分章节，章节下可划分场景。
- 场景记录时间、地点、人物、目标、事件、情绪、前置状态、结束状态、正文和插图描述。
- 最终阅读内容由故事页面及其可排序内容块组成。

### 3.4 素材管理

素材管理是首个实施模块。故事正式发布前，相关素材均在管理端准备、整理和确认，不直接向使用端展示未发布内容。

#### 3.4.1 素材分类

首期至少包括：

- 人物表情：人物在不同情绪或状态下的面部参考
- 人物立绘：人物全身、半身或不同姿态的形象参考
- 人物服装：人物在不同时间、场景或篇章中的服装参考
- 综合素材：暂时无法归入专门分类，或同时包含多种信息的参考素材
- 完整图片：可作为完整画面查看、编排或发布的成品图片，包括场景插图和 CG

素材分类后续允许扩展地点、物品、场景、封面和视频等类型，不应在数据库中写死为无法扩展的固定列。

#### 3.4.2 素材基本操作

- 上传单张或批量上传图片
- 预览图片及查看基本信息
- 修改素材显示名称，文件存储名称与显示名称分离
- 编辑素材说明、标签及其关联对象
- 删除、恢复或归档素材；删除前必须提示其正在被哪些内容引用
- 在同一分类内通过拖拽改变素材显示顺序，例如将位置 1 拖到位置 2
- 在不同分类之间拖拽移动素材，例如把“人物表情”移动到“人物立绘”
- 跨分类移动只改变分类和排序，不重复上传或复制底层文件
- 拖拽完成后立即显示新位置；保存失败时恢复原位置并提示原因
- 支持多选后批量移动、批量打标签和批量关联（可在首期拖拽功能稳定后实现）

分类列表及分类内素材都应拥有稳定的排序字段。排序以同一故事系列、同一分类为作用范围，避免一个系列的调整影响其他系列。

#### 3.4.3 素材快速切换与预览器

素材页面需要提供一个用于查看当前素材的大尺寸主预览框，并在主预览框下方显示当前结果集中的多个小尺寸缩略图框。

- 主预览框始终展示当前选中的一份素材，并显示其名称和当前序号
- 点击任意缩略图后，主预览框立即切换为该缩略图对应的素材
- 当前缩略图应有清晰的选中状态，例如高亮边框
- 提供序号输入框；输入有效序号并确认后，立即切换到对应素材
- 序号按当前筛选、搜索和排序结果从 1 开始计算，而不是使用数据库 ID
- 序号输入应只接受正整数；为空、超出范围或格式错误时不切换，并给出明确提示
- 切换到当前可视区域外的素材时，下方缩略图区域应自动滚动，使选中项可见
- 支持上一份和下一份切换；到达首尾时按钮禁用，不默认循环
- 素材数量较多时，缩略图区域应支持横向滚动或分页加载，但主预览切换不能要求重新打开页面
- 搜索、筛选、分类切换或拖拽排序后，应重新计算展示序号，并尽量保持原素材为当前选中项
- 如果当前素材因筛选或跨分类移动而离开结果集，则自动选择新结果集的第一项；结果为空时显示空状态
- 切换素材只改变当前预览和编辑目标，不改变素材分类、排序或 AI 参考选择状态

#### 3.4.4 素材归属与引用

素材首先归属于某个故事系列，并可进一步关联人物、篇章、章节、场景、地点或物品。一份素材可以被多个业务对象引用；“移动分类”和“修改名称”不得导致已有引用失效。

人物表情、人物立绘和人物服装应优先关联具体人物。服装还可以标记适用的篇章、时间段或场景。完整图片可以标记为内部参考、故事插图、封面或 CG。

#### 3.4.5 AI 参考素材

生成文字或图片前，管理员可以从素材库明确选择本次任务使用的参考素材。系统不得默认把系列内全部素材无差别发送给 AI。

- 支持按分类、人物、篇章、场景和标签筛选参考素材
- 支持拖拽或勾选素材加入“本次 AI 参考区”，并可调整参考顺序
- 每份参考素材可设置参考用途，例如人物外貌、表情、服装、姿态、场景、构图或整体画风
- 可为参考素材设置说明和参考强度；具体强度参数根据未来接入的 AI 模型适配
- 图片生成应优先遵守所选人物形象、服装和场景参考
- 文字生成可读取素材名称、说明、标签和关联设定；是否直接读取图片由所选模型能力决定
- 每次生成记录必须保存实际使用的素材 ID、素材版本、顺序、用途和生成参数，以便复现和追溯
- 素材后续被改名、移动或更新时，不应改变历史生成记录引用的素材版本

#### 3.4.6 素材字段与文件管理

每项素材应记录：

- 素材 ID、显示名称、素材分类、排序值和状态
- 原始文件名、文件地址、文件类型、尺寸、大小和校验值
- 来源、版本、上传者、创建/更新时间和版权说明
- AI 模型、提示词及生成参数（如适用）
- 关联的系列、篇章、场景、人物或地点
- 是否公开、是否属于 CG、是否允许下载、是否涉及剧透

文件本身存储于文件系统或对象存储，数据库保存地址和元数据，不直接存放大型图片或视频二进制。

#### 3.4.7 素材管理页面首期布局

- 左侧：故事系列选择和素材分类列表
- 中间：大尺寸主预览框、序号输入与上一份/下一份切换，以及主预览框下方可点击和滚动的缩略图列表；素材网格支持拖拽排序、选择和跨分类拖动
- 右侧：选中素材的预览及名称、说明、标签、关联人物/篇章等属性编辑
- 顶部：上传、搜索、筛选、批量操作和进入 AI 参考选择的入口

首期验收标准：管理员能够上传图片、预览、重命名、通过序号或缩略图即时切换主预览、在分类内拖拽排序、跨分类拖拽移动，并为一次 AI 任务选择有顺序和用途的参考素材；刷新页面后所有持久化结果保持一致。

### 3.5 AI 创作工作流

1. 读取系列全局规则。
2. 读取篇章局部设定。
3. 读取场景涉及的人物、地点和时间线。
4. 读取前文摘要和人物当前状态。
5. 检查设定冲突。
6. 生成大纲、正文或插图提示词。
7. 管理员审核、修改、采用或重新生成。
8. 依据确认内容生成插图，未来可生成视频。
9. 保存生成记录与版本。
10. 审核通过后发布给使用端。

### 3.6 管理端页面规划

- 管理概览
- 故事系列管理
- 世界设定库
- 人物、地点、组织、物品和术语管理
- 故事线与时间线管理
- 篇章、章节、场景和故事页面编辑器
- AI 文字创作工作台
- AI 插图生成工作台
- 素材与 CG 库
- 版本、审核和发布管理
- AI 模型与提示词配置

## 4. 使用端需求

首期默认全部公开，不考虑登录、用户权限、付费、收藏或评论。

### 4.1 故事发现与筛选

- 展示推荐、最新、热门故事及系列基本信息
- 按故事名称、关键词、主题、题材、标签、主角、世界观类型、更新状态筛选
- 提供“浏览主角”入口：主角列表 → 公开人物详情 → 所属系列和相关篇章

### 4.2 系列和篇章预览

- 系列封面、名称、公开简介和公开世界观
- 主角及主要人物、主题标签、公开 CG 和更新状态
- 篇章封面、简介、顺序、章节数量和发布时间
- 开始阅读及未来的继续阅读入口

### 4.3 图文阅读器

- 按页面或场景展示插画、文字描述和人物对白
- 上一页、下一页、章节目录、阅读进度和全屏展示
- 内容块按管理端配置的顺序呈现
- 数据模型预留视频内容块，视频不纳入首期开发

### 4.4 CG 图鉴

- 按系列展示 CG 缩略图
- 按篇章、人物、地点或主题筛选
- 大图、全屏、上一张/下一张和图片说明
- 仅当素材被标记为公开且允许下载时提供原图下载
- 剧透 CG 应具备隐藏或提示能力

## 5. 核心业务关系

```text
故事系列
├── 世界设定
├── 人物 / 人物关系
├── 地点 / 地点关系
├── 组织 / 物品 / 术语
├── 故事线 / 时间线
├── 篇章
│   ├── 篇章局部设定
│   └── 章节
│       └── 场景
│           └── 故事页面
│               └── 可排序内容块（文字 / 对话 / 插画 / 未来视频）
└── 素材库
    ├── CG
    ├── 人物素材
    ├── 场景素材
    ├── 物品素材
    └── 未来视频素材
```

## 6. 首期开发范围（MVP）

实施顺序首先完成管理端素材管理模块，再扩展其余故事工作流：

1. 素材管理：上传、预览、重命名、分类、排序、跨分类拖拽和 AI 参考素材选择。
2. 管理端创建和维护故事系列。
3. 录入世界观、人物、地点和基本设定。
4. 创建篇章、章节、场景和故事页面。
5. 上传插画并编排图文内容块。
6. 审核并发布故事。
7. 使用端按主题或主角筛选系列。
8. 查看系列详情、篇章列表并阅读图文内容。
9. 浏览和下载被允许公开下载的 CG。

首期不包含：登录权限、付费、收藏、评论、复杂推荐、正式 AI 生成接入和视频生成/播放。数据结构应为 AI、视频和权限扩展预留空间。

## 7. 后续设计工作

- 设计数据库实体关系、继承/引用方式和版本模型
- 定义草稿、审核、发布、下线等状态流转
- 定义管理端与使用端 API 边界
- 设计 AI 上下文组装、摘要、冲突检测和成本控制方案
- 明确 CG 下载规格、图片压缩及水印策略
- （已完成）选择数据库与本地文件存储方案 —— 见第 8 节

## 8. 已确认的技术决策（本地开发）

> 更新时间：2026-08-12

- **数据库**：MySQL（本机实例）。库名 `story_admin`；连接账号写入本地 `application.yml`（勿提交密钥）。
- **文件存储（首期）**：项目路径下 `storage/`（不接对象存储，**纳入 Git 管理**）。数据库只保存相对路径与元数据；管理端变更文件后应提交 `storage/`。后续若迁移 OSS，仅替换存储实现与路径前缀。
- **素材模块设计**：见 [`docs/superpowers/specs/2026-08-13-asset-module-design.md`](docs/superpowers/specs/2026-08-13-asset-module-design.md)。
- **组合编排设计**：见 [`docs/superpowers/specs/2026-08-13-asset-combo-design.md`](docs/superpowers/specs/2026-08-13-asset-combo-design.md)。

## 9. 尚未确定的事项

以下内容尚未定案，开发时不得自行假定：

- 首批接入的文本与图片 AI 模型
- 故事的审核角色与具体发布流程
- “热门”和“推荐”的计算规则
- 剧透内容的解锁方式
- 视频功能的具体阶段和格式
- 后续用户账户、权限和商业化方式
- 生产环境的对象存储与部署方式（首期仅本地目录）

## 10. 文档维护规则

- 已确认需求写入相应章节。
- 尚未确认的想法写入“尚未确定的事项”，不可表述为既定功能。
- 范围变化时同步修改“MVP”和相关功能章节。
- 数据库及 API 设计完成后，在 `docs/` 下新增专项文档，并在本文档中添加链接。
- 每次修改应使用能说明需求变化的 Git 提交信息。

