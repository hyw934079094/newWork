# 人物身高与素材挑选优化设计说明

> 状态：已实现（首期）  
> 日期：2026-08-14  
> 分支策略：在 `master` 上实现

## 1. 背景与目标

人物编辑弹窗中「从素材库指定」目前是纯文字多选下拉，素材变多后难以辨认与定位。同时人物缺少身高字段；删除人物时若仅关联已进回收站的素材，界面显示无素材，删除却报 409。人物管理页筛选/表格/弹窗排版拥挤、易莫名换行。

本期目标：

1. **人物身高**：可选整数厘米字段，列表可读展示。  
2. **素材挑选**：人物编辑内用可筛选、带缩略图的二级弹窗替换下拉。  
3. **删除一致**：删人物时只被 NORMAL 关联挡住；DELETED 幽灵关联自动清理。  
4. **人物页美化（首期 A）**：整理筛选区、表格操作、编辑/挑选弹窗排版与交互；其它管理页后续逐个做。

## 2. 已确认决策

| 项 | 决策 |
|----|------|
| 范围 | 仅人物编辑「从素材库指定」+ 人物管理页美化（不做本体/组合统一组件；不做整站换肤） |
| 身高存法 | `height_cm` 整数厘米，可空；展示「168 cm」 |
| 挑选筛选 | 分类下拉 + 关键字（显示名）；不做人物关联筛 |
| 挑选交互 | 二级弹窗 + 缩略图网格多选 |
| 确定后写库 | 确定只更新本地已选；仍点「保存关联」提交（与现网一致） |
| 删人物 | 仅 NORMAL 关联 → 409；仅 DELETED → 清 rel 后删人 |
| 回收站断链 | 本期不做（素材进回收站不自动断人物关联） |
| 分页 | 本期不做 |
| 实现路径 | 人物页内嵌弹窗 + 复用 `listAssets`；不抽通用组件、不加专用缩略图 API |
| UI 推进顺序 | 先 A 人物管理；其它页面以后一个一个来 |

## 3. 人物身高

### 3.1 数据

- 表：`character_profile`  
- 列：`height_cm INT NULL`（COMMENT：height in centimeters）  
- Flyway：`V5__character_height_cm.sql`

### 3.2 API

- 创建/更新请求与人物 JSON 增加 `heightCm`（Integer，可空）  
- 有值时服务端校验：整数且 ∈ [1, 300]；否则 400  
- 列表/详情原样返回

### 3.3 UI

- 人物编辑表单：输入「身高 (cm)」，可空  
- 列表列：有值显示 `{n} cm`，空显示 `-`  
- 本期不做按身高筛选

## 4. 素材挑选弹窗

### 4.1 入口

- 人物编辑「人物预览素材」区：去掉纯文字 `el-select`  
- 按钮「从素材库指定」打开二级 `el-dialog`（约 720–800px）  
- 已关联缩略图区、上传预览图、「保存关联」保持不变

### 4.2 筛选与数据

- 顶栏：分类（含「全部分类」）+ 关键字  
- 请求：现有 `GET /api/assets`，参数 `status=NORMAL`，可选 `categoryId`、`q`  
- 不传 `characterFilter` / `characterId`  
- 变更筛选立即重新拉取；本期不分页

### 4.3 网格与选择

- 缩略图 + 显示名；点击切换勾选  
- 打开弹窗时以当前已选/已关联 id 预勾选  
- 底栏：已选数量、取消、确定  
- 确定：关闭弹窗并写回本地 `selectedLibraryIds`（或等价状态）；不自动调用保存关联 API

### 4.4 非目标（挑选）

- 不抽到组合/本体  
- 不新增缩略图专用端点  
- 不在选项下拉里塞小图

## 5. 删人物与幽灵关联

### 5.1 行为

`CharacterService.delete(id)`：

1. 取该人物全部 `asset_character_rel` 对应素材  
2. 若存在任一 `status=NORMAL` → `409 Conflict`，文案仅列 NORMAL 素材 id  
3. 否则（无关联，或仅 DELETED）→ 删除该人物全部 `asset_character_rel`，再删人物行

### 5.2 展示

- `listAssets`（人物侧）仍只返回 NORMAL，与现网一致  
- 本期不在编辑页展示「回收站关联」

### 5.3 测试

- 仅挂 DELETED 素材的人物 → 删除成功，rel 清空  
- 挂 NORMAL 素材 → 仍 409  
- 身高边界：null / 合法 / 非法

## 6. 人物页排版与交互美化（首期 A）

仅改 `CharacterList.vue`（及必要局部样式），沿用现有 Element Plus 色板。

| 区域 | 要求 |
|------|------|
| 筛选区 | 工具条网格：固定行高与间距；字段横向对齐，避免 label/输入被挤成一字一行；查询/重置成组右对齐 |
| 表格 | 次要列收窄；操作列主操作「预览」「编辑」+「更多」下拉（添加形态/删除），减少折行挤压 |
| 编辑弹窗 | 宽约 840px；基础字段两列网格（姓名/别名、故事/身高、性别/年龄、种族/职业）；简介/说明整行；素材区独立分区；已关联缩略图统一尺寸、标题单行省略 |
| 挑选弹窗 | 顶栏筛选一行不挤；网格固定卡片；选中清晰描边；底栏已选数+取消/确定 |
| 视觉边界 | 不做整站换肤、不做花哨动效；其它管理页不在本期 |

## 7. 验收标准

1. 可保存/展示人物身高（含空）；非法值拒绝  
2. 人物编辑可按分类+关键字筛选并看见小预览图多选素材；确定后「保存关联」写库成功  
3. 「熊猫头」类仅幽灵 DELETED 关联的人物可删除成功  
4. 仍有 NORMAL 关联时删除仍失败且提示可见素材  
5. 人物页筛选/表格/编辑弹窗无明显一字换行；操作区不因按钮过多严重折行；挑选弹窗布局整齐可用

## 8. 非目标

- 通用 AssetPicker 组件推广到组合/本体  
- 素材进回收站自动断人物关联  
- 按身高筛选人物  
- 挑选列表服务端分页 / 缩略图 CDN  
- 整站壳子/其它业务页美化（后续逐页）

## 9. 实现备注

- Git：`D:\tool\Git\bin\git.exe` + `-F` UTF-8 无 BOM；`master` 直接提交  
- JDK / Node：沿用项目约定（`JAVA_HOME=D:\jdk\jdk-24.0.1`，Node `D:\tool\nvm\v22.17.0`）  
- TSD：改 `.java`/`.sql`/`.md` 后验明文；必要时 `.txt` + `cmd ren`
