# 素材小图公共组件 + 组合播放序列可空

> 状态：已确认  
> 日期：2026-08-16

## 决策

1. `AssetThumb`：小图点击 → `ImageLightbox`（滚轮缩放 + 拖动）  
2. 组合编辑：可选列表与已选成员均展示 `AssetThumb`  
3. `playSequence` 可空；空则按成员顺序默认 `1..n`（前后端一致）  
4. 本轮优先接组合；系列/篇章列表封面等顺手改用 `AssetThumb`
