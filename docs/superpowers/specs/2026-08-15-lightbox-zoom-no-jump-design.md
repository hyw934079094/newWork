# 素材翻页不跳页 + 大图滚轮缩放

> 状态：已确认  
> 日期：2026-08-15

## 1. 上一页/下一页

- 去掉会带动整页的 `scrollIntoView`
- 仅横滚 `thumbsScroller`，使当前缩略图进入可视区

## 2. ImageLightbox

- 共用组件：遮罩点空白关闭；滚轮 0.5×～4× 居中缩放；关闭复位
- 接入：素材工作台、系列封面、篇章预览封面、PagePreview 画面组、人物预览（替换 el-dialog，保留上下张）
